package com.revature.models;

/**
 * Enum is a custom type in Java that allows us to define a set of constants
 * 
 * These enum values MUST match in casing to what we have defined in our database
 */
public enum Role { Admin, Customer, Employee}
